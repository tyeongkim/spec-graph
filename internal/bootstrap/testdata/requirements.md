# REQ-001 All APIs require authentication
This requirement ensures that every API endpoint validates tokens.

# REQ-002 Rate limiting on public endpoints
Public endpoints must enforce rate limits.

REQ-003 is mentioned here but without a heading.
